# 远见工作台进度

## 当前阶段

报告可视化、按需商品推荐与报告页原地阅读已实现并验证。本轮继续通过 PR #3 交付：用户在报告页明确发起商品查询，已保存结果不重复消费。团队以合并后的上游 `main` 同步。

## 已完成

- 已核对上游 main 的启动入口、Windows 支持、Codex 配置和工具隔离实现。
- 已整理本地升级范围；项目数据、导出报告和设备配置不进入公开仓库。
- 已合入快速／A～Z／意图深挖、1～6 轮采词及按平台、按轮保存的预算与结果。
- 已保留七项可选洞察、具体人群画像、逐块生成和重跑、历史报告与完整 HTML／Markdown 导出。
- 已保留远端 Codex 程序选择、provider 和认证配置、MCP 逐项禁用及失败保护。
- 已修复公开源码首次初始化和双击启动：支持零项目，不再依赖私有资料包的项目编号。
- 本地仓库已初始化为 main，关联上游与 Fork；已有项目和导出文件校验值未改变。
- 已使用团队入口实际启动，健康接口、全部现有项目和静态资源均返回 HTTP 200，内容与本地一致。
- 浏览器验证采词方式、轮次与预算控件、采集后洞察选择入口，以及已有画像和完整报告读取。
- 报告改为结论先行、数量与分布图、按人群折叠的完整画像及行动建议；取消逐词分类和逐词内容建议展示，保留后台汇总和原始资料。
- 「选品建议」保留最多 3 个带依据的产品方向；用户在报告页明确点击后查询卖家精灵 Amazon 美国站商品，由本机 Codex 选出一个优先参考商品和最多两个备选。旧报告可按需生成，不必返回电商盯盘或重跑整份洞察。
- 每份报告一次推荐最多查询两个英文词、每词十件商品；同一份报告及其内容持久复用结果。失败、中断和无匹配不自动重试；导入恢复不重启外部调用，模型引用限定在保存的商品与原文快照内。
- 报告页优先显示中文产品方向、商品数据和对比理由；完整洞察、关键词报告、原文、商品资料和趋势均原地查看。范围说明与核验清单默认折叠。
- 商品推荐与 AI 分析互斥执行；用户发起后，前端忙碌等待只读轮询。跨项目、版本切换和乱序加载不会写回错误项目或发起错误推荐，旧内容书签继续兼容。
- 电商盯盘新增商品行、小趋势图、指标切换和历史快照回看；图表按真实日期绘制，区分零值与缺失，父子体销量分别展示。折叠明细保留月度 6 项数值；日价格查询失败时，同 ASIN 的月均价可以明确标注口径后展示。
- HTML 导出内置图表和版式，保留折叠画像与原始依据。导出结构回归通过：七模块内容、完整画像、产品方向、来源保护和零外部调用均有覆盖。

## 最近验证

- 公开源码 Python 回归共 200 项：198 项通过、2 项按当前环境跳过；推荐后台和本机 HTTP 集成测试使用临时数据和模拟模型、供应商，不消耗外部额度。
- 10 组 JavaScript 回归全部通过；新增商品推荐专项覆盖缓存、忙碌排队、跨项目等待恢复、请求乱序、报告版本一致、原地弹窗及导出。真实导航函数测试保留旧书签兼容断言。
- 初始化验收通过：13 个 Python 文件语法、全部前端资源、10 个项目版本校验和及 HTTP 读取通过。
- PR 提交者记录：经用户授权，以已有饮水机洞察的 205 条证据执行真实链路；本机 Codex 规划与排名成功，卖家精灵商品查询 1 次、HTTP 200、返回 10 件、跳过 0 件，得到 1 件优先参考商品和 1 件备选。当前合并审查未重复调用外部服务。
- 浏览器验证推荐资料、原文及趋势弹窗保持在报告页；刷新、原地查看与导出后推荐编号及一次查询预算不变。真实推荐经内存导入恢复后快照一致。
- 通过 Chrome 实际下载完整 HTML，保存为本机忽略目录 `exports/yuanjian-auto-selection-report.html`（596743 字节）；文件包含商品推荐、完整洞察、画像、图表和原文，8 个推荐商品及证据锚点均存在，不含脚本、工作台操作按钮或远程商品图片。独立打开后原文链接能定位并展开；桌面与 390px 宽度下无横向溢出。

## 待办

- 本轮功能与验收已完成，源码按既有 PR 流程提交。
- 团队评审并合入本轮报告可视化与选品更新。

## 验证边界

- 真实验收记录覆盖已有报告的手动推荐；新洞察完成后不会自动续接。主动生成、失败和并发边界通过模拟回归，本轮合并审查未付费重跑整份七模块洞察。
- 商品推荐来自有限查询与需求样本，性能、适销性、成本和利润未验证。销量为供应商估算，未注明月份时保留缺失说明；历史曲线仍按需手动读取，未开启定时监控。
- 浏览器打印及 PDF 输出尚未实测。
- Windows 启动改动需与现有脚本和测试对照；macOS 环境不能代替 Windows 实机验证。
- Git 仅同步源码，成员间的业务数据通过应用导入、导出交接。

## 2026-09-12 PR #3 合并复验
- 最新提交重新审查后，200 项 Python 回归通过，其中 2 项按环境跳过；10 组 JavaScript 回归通过；公开源码初始化验收通过。
- 商品推荐改为用户在报告页明确点击后启动；打开项目、切换报告、导出和洞察完成均不自动调用外部服务。
- 推荐缓存同时核对报告内容与证据快照，同一报告重跑选品模块后不会展示旧商品结论；旧报告范围提示已恢复。
- 本次复验只使用临时或合成资料，未调用真实供应商或模型服务。
