# 远见工作台进度

## 当前阶段

已基于上游 main 完成功能整合和本机验收，等待团队评审合并；保留团队启动与 Codex 兼容处理。

## 已完成

- 已核对上游 main 的启动入口、Windows 支持、Codex 配置和工具隔离实现。
- 已整理本地升级范围；项目数据、导出报告和设备配置不进入公开仓库。
- 已合入快速／A～Z／意图深挖、1～6 轮采词及按平台、按轮保存的预算与结果。
- 已合入七项可选洞察、具体人群画像、逐块生成和重跑、历史报告与完整 HTML／Markdown 导出。
- 已保留远端 Codex 程序选择、provider 和认证配置、MCP 逐项禁用及失败保护。
- 已修复公开源码首次初始化和双击启动：支持零项目，不再依赖私有资料包的项目编号。
- 163 项 Python 回归测试通过，7 组 JavaScript 回归通过；空数据初始化检查通过。
- 本地仓库已初始化为 main，关联上游与 Fork；已有项目和导出文件校验值未改变。
- 最终工作目录 172 项 Python 测试通过，其中 9 项为本机保留、不公开提交的旧打包测试。
- 已使用团队入口实际启动，健康接口、全部现有项目和静态资源均返回 HTTP 200，内容与本地一致。
- 浏览器验证采词方式、轮次与预算控件、采集后洞察选择入口，以及已有画像和完整报告读取。

## 待办

- 团队评审并合入上游 main。

## 验证边界

- 本轮 Git 整合只做本地和模拟验证，不自动调用第三方采集或 AI 服务，不消耗供应商额度。
- Windows 启动改动需与现有脚本和测试对照；macOS 环境不能代替 Windows 实机验证。
- Git 仅同步源码，成员间的业务数据通过应用导入、导出交接。

## 2026-09-12 调研流程清晰化（待 PR 合入）

- 已调整首屏价值说明、三入口名称、分阶段下一步与报告阅读顺序。
- 已统一新版关键词库的汇总入口及完整 HTML 导出；保留旧报告与证据。
- 8 组 JavaScript 回归通过；桌面与手机浏览器检查通过，首屏按钮溢出已修复。
- Python 167 项：165 通过、1 跳过、1 既有平台断言失败；同一失败已在未修改上游复现，详见 docs/verification/2026-09-12-flow-clarity.md。
- 团队入口与本地只读 HTTP 验证通过；无真实第三方调用，无线上部署。
- 关注与候选拆分、市场证据字段及行动卡留待后续，不宣称本轮完成。

## 第二轮前端交付

- 首页左右任务分区、来源说明、青绿色视觉、手机文字导航和数据源入口完成。
- watched 与 candidate 分离：独立筛选与保存，旧项目默认未关注，保留原候选；关注不使分析过期，候选变动仍更新数据版本。
- 报告顶部行动卡读取已保存综合判断，显示原因、交付物、验收方法与证据；无建议时显示空态，不虚构结论。
- 9 组 JavaScript 回归通过；Python 168 项中 166 通过、1 跳过、1 既有平台断言失败。
- 临时演示目录的真实浏览器关注切换、桌面与 390px 手机检查通过；未调用第三方服务，未部署线上。

## 2026-09-12：拿词后的人群策略主线

- 根据最新需求，将左侧主导航改为五步决策视图；竞品移为辅助入口。
- 同一份分块报告、同一人群选择贯穿场景、八层洞察、定位与验证问题；保留原始证据、来源范围、数据过期提示。
- 千机塔按统一八层展示；旧报告的深层情感、价值观不冒充隐秘欲望。第八层暂为待验证，完整历史报告不迁移、不覆盖。
- 项目级综合判断、选题明确标注，不伪称与当前人群建立了逐条关系。
- 前端模拟回归与本机界面检查，不代表真实模型分析、内容投放或成交验证。

## 2026-09-12：补齐下拉词采集与七步清洗

- 左侧增加独立第 1 步下拉词采集，第 2 步关键词清洗；已有词时，下一步优先提示清洗而非跳到评论。
- 展示知识库采词路径与七步清洗方法，独立清洗按钮复用已有 clean 分块接口；保持在关键词页等待结果。
- 清洗结果按词库、样本统计、排除词与原因、主题入口呈现。保留证据编号；可核对时显示原始查询与轮次，历史缺失字段不补造。
- 边界：封面提词、真假流量核对仍需人工；本轮未把排除词自动从后续人群模型输入中剔除，页面明确提示。
- 11 组前端模拟回归通过；未触发真实采集与模型调用。

## 2026-09-12 人群策略三步
- 主导航收紧为整理词库、拆分人群、分人群策略；采集与清洗保留在词库入口。旧书签重定向，手工决策数据保留，不强制填写六模块。
- 新增 segments 模块，在同一人群对象内保存购买动机、人生阶段与产品/渠道/内容/达人/促销建议。旧报告不补造缺失方案，来源编号继续校验。
- 离线回归覆盖新 schema、旧报告兼容、跨人群切换及导出；本地浏览器验收三项导航与 628px 窄屏。未调用真实采集或模型，未部署公网。
- 已知限制：关键词清洗排除标记尚未限制后续模型取样；生成结果质量与真实渠道效果仍待实际数据验证。

## 2026-09-12 恢复分析深度
- 三步导航保留，人群页增加横向对照、可展开深描；策略页恢复左侧人群列表、右侧详细方案与场景决策过程。
- 无报告时展示真实资料数量、原词预览及准备入口，不捏造人群方案。
- 12组离线前端回归通过；真实浏览器628px无横向溢出。未调用采集或模型；有结果切换由离线夹具验证。

## 2026-09-12 消费动机分析 Skill 本地适配
- 将消费动机分析方法作为项目级 Skill 纳入 skills/consumer-motivation-insight；不安装全局、不覆盖已有采集 Skill。
- segments 生成提示实际加载 analysis.md，规则参与已有提示版本哈希；clean 等模块保持原逻辑。九维映射到现有画像字段，保持旧报告 schema 兼容。
- 页面提供方法与来源说明。仅分析现有项目资料，不声称拥有原私有检索数据库，历史报告不自动重写。
- 验证实际提示加载、非目标模块隔离、现有schema与前端；未真实调用采集或模型。

## 2026-09-12 分组导航
- 按收集整理、理解人群、制定策略、资料工具四组展示11个入口，三步只作为工作流提示，不再限制导航数量。
- 九维、人群深描、内容方向及Skill各自独立视图，复用同一报告与人群选择；缺少独立字段时明确提示，不拆造旧报告。
- 12组前端基线与新版测试通过，实际浏览器验证菜单、高亮、独立标题及628px无横向溢出。未触发模型或采集。

## 2026-09-12 洞察名称与品牌展示
- 页面统一使用“消费动机洞察”，移除第三方方法品牌和对应公开外链；内部方法溯源保留，分析逻辑不变，旧书签仍可访问。
- 12组前端回归通过，添加名称与品牌展示回归。

## 2026-09-12 源码命名清理
- 分析方法目录、加载路径、测试类名、文档和方法元数据统一为中性命名；清理关联外链与记录编号。
- 分析规则不变，旧历史提交保留；源码交付包只包含当前版本文件。

## 2026-09-12 证据驱动分析第一阶段
- 分人群策略生成前，仅纳入匹配当前版本与原词快照的有效关键词。未清洗、排除、过期和未覆盖词不进入该模块；原帖评论独立取样，源数据不删除。其他旧分析工具保持原范围。
- 新版策略人群新增9项独立motives字段，每项保存判断、依据性质、证据编号和验证方法；历史报告缺项只补未知标记，模型新输出必须完整符合schema。
- 新增分析资料清单、需求证据台账两个入口，导航共13项。台账显示同一人群动机与原始输入摘录，完整报告导出也包含新字段。
- 离线验证实际runner输入排除、过期/变更快照拒绝、恢复兼容、字段导出及页面来源关联。13组前端通过；177项Python中175通过、1跳过、1既有macOS端口断言失败。未调用真实模型或采集。
- 后续：人工确认/合并拆分、可执行策略清单、需求地图尚未实现。

## 2026-09-12 浅红主题与名称
- 需求证据页面统一命名为需求洞察库。
- 品牌主题调整为浅红背景、浅红选中态与深红交互按钮；保留成功/警告语义色，业务逻辑不变。
- 前端回归通过，真实浏览器核对标题、计算颜色与628px无横向溢出。

## 2026-09-12 PR #2 合并复验
- 最新提交重新审查后，177 项 Python 回归通过，其中 2 项按环境跳过；13 组 JavaScript 回归通过；公开源码初始化验收通过。
- 修正关键词清洗规则说明、独立清洗报告导出、重复证据锚点和手机端辅助导航布局。
- 本次复验只使用临时或合成资料，未调用真实采集、供应商或模型服务。
