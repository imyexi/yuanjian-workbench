# 第二轮前端验证

## 实际完成

首页左右分区与统一青绿色样式；手机文字导航和设置入口；关注 watched 与候选 candidate 独立；行动卡读取已保存的 summary.actions，不调用模型。行动卡和完整 HTML 均保留来源、范围和旧版本提示。

## 测试

- `for f in tests/*.js; do node "$f" || exit 1; done`：退出 0，9 组通过。
- `NO_PROXY=127.0.0.1,localhost no_proxy=127.0.0.1,localhost python3 -m unittest discover -s tests -v`：168 项，166 通过、1 跳过、1 既有失败（SO_REUSEADDR 实际 4，预期 1；前轮已在未修改上游复现）。
- 新增 HTTP 测试验证：关注不改变候选与 data_version；候选变更保留关注且更新版本；过期 revision 返回 409；任意字段修改返回 400；导出恢复保留关注，旧项目恢复默认未关注。
- 新增行动卡测试验证：字段、精确证据、旧数据提示、空态、HTML 转义、完整导出。
- 浏览器桌面 1440×1000、手机 390×844：关注从 2 变 3，候选保持 3，再取消恢复；手机行动页宽度和内容宽度均为 390，无横向溢出；数据源与指南保持可达。
- 截图来自临时目录的演示项目，明确演示标记，非真实市场报告。

## 运行

Python 3.10+：`python3 team_start.py --open`。本机预览由团队入口在回环地址启动；源码包不含账号、连接配置与业务项目。

## 回退边界

回退代码不删除项目历史；旧代码忽略 watched 新字段。若使用旧版导入恢复功能，关注标记可能不保留；完整新版项目备份应继续保存。候选字段和旧数据结构保持兼容。无线上部署与付费采集。
