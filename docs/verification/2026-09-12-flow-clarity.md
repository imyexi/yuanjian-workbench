# 调研流程清晰化验收

## 交付范围

- 三入口更名为需求调研、竞品研究、结论与行动；原 URL、数据格式与业务接口不变。
- 首屏说明产出与目标市场，研究来源和采集参数折叠，预算与费用提示保持可见。
- 空资料、只有关键词、已有内容、已有评论分别给出下一步；不自动采集或分析。
- 已有洞察报告优先展示，生成配置后置并保留展开状态。
- 汇总页与完整 HTML 导出识别新版关键词库，保留旧版报告和过期提示；打开结果不请求模型。
- 手机端主按钮竖排，修复横向溢出。

## 验证方式

基线为上游提交 `1c239432be5861c32e6b0d39271b4feae2dc436c`。新增测试先在旧代码失败，再验证修改后通过。

```sh
for f in tests/*.js; do node "$f" || exit 1; done
NO_PROXY=127.0.0.1,localhost no_proxy=127.0.0.1,localhost python3 -m unittest discover -s tests -v
python3 team_start.py --port 8876
python3 scripts/check_project.py --url http://127.0.0.1:8876
git diff --check
```

使用 Node 24 与随 Codex 提供的 Python 3.12。系统自带 Python 3.9 低于项目要求，未用于运行服务。

- JavaScript：8 组通过，涵盖新增指引、汇总、导出和已有采集、IME、历史报告、商品趋势测试。
- Python：167 项中 165 通过、1 跳过、1 失败。失败为 `test_posix_listener_keeps_address_reuse_enabled`，本机套接字选项返回 `4` 而测试严格断言 `1`。在独立的未修改上游工作树使用相同环境复现同一失败，本轮未修改后端，也未放宽测试。
- 首次 Python 测试受本机代理影响；限定本次测试的回环地址不走代理后，7 个 HTTP 错误消失。未修改系统代理配置。
- 团队入口真实启动；空项目初始化、健康接口和全部静态资源 HTTP 验证通过。
- 浏览器：桌面 1440×1000、手机 390×844；手机首屏和洞察报告 `scrollWidth=clientWidth=390`。设置切换平台后仍展开。汇总打开精确关键词库章节，已保存报告位于分析配置之前、配置默认折叠。
- 浏览器报告使用临时目录中的明确演示数据，不是真实市场样本。没有触发采集、翻译、模型生成或趋势更新。

## 保留的后续工作

关注竞品与候选商品分离、五入口重组、跨市场证据字段、行动卡生成均未包含在本 PR。当前优先交付无需迁移数据的第一轮改进。Windows 未做实机验证，线上未部署。

## 回退

本轮不迁移数据。合并后可对本轮提交执行 `git revert`，保留历史和业务数据；不使用重置或强推。
