# 人群策略三步交付
沿用用户已明确的任务：采集与清洗作为输入，按动机拆分人群，再逐人群生成营销策略。
1. 保留旧工作树与历史数据，三项导航替换六项导航，旧书签重定向。
2. 新增独立 segments 模块，不改变旧画像 schema。策略与人群存入同一对象，避免名称猜配。
3. 页面允许查看来源、切换人群、主动生成；空缺字段明确显示待生成，不以示例伪造结果。
4. 运行离线回归、真实 HTTP 与浏览器检查，提交原 PR，源码打包不含私有资料。
限制：本轮不接入 外部私有数据库，不自动调用模型；清洗排除暂不自动缩减模型采样。
