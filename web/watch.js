/* Amazon competitor discovery and explicitly requested history snapshots. */
const watch={projectId:'',productId:'',historyIndex:-1,metric:'price',months:'12',filter:'all',busy:false,modalView:null,modalSequence:0};
function latestHistory(id){return [...(state.project?.watch_history||[])].reverse().find(x=>x.product_id===id)}
function watchValue(value,decimals=0){return typeof value==='number'&&Number.isFinite(value)?value.toLocaleString('zh-CN',{maximumFractionDigits:decimals}):'未提供'}
function historyResult(history,tool){return history?.results?.find(x=>x.tool===tool)}
function lastPoint(points){return [...(points||[])].reverse().find(x=>typeof x.value==='number'&&Number.isFinite(x.value))}
const WATCH_METRICS={price:['价格','历史价格 · USD',2],bsr:['BSR','BSR 排名',0],rating:['评分','评分 · 5 分制',1],ratings_count:['评分数','评分数量',0],child_units:['子体月销量','子体月销量 · 第三方估算',0],parent_units:['父体月销量','父体月销量 · 第三方估算',0]};
function watchReportStyles(){return `
.watch-page,.watch-export{--watch-accent:#1765d1;--watch-line:#e6e9ed;color:#26313c}.watch-page .caption,.watch-export .caption{font-size:12px;line-height:1.8;color:#78818a}.watch-page .page-head{margin-bottom:24px}.watch-page .watch-detail{margin:0 0 30px!important;padding:24px 0;border-top:1px solid var(--watch-line);border-bottom:1px solid var(--watch-line);scroll-margin-top:140px}.watch-detail-head{display:flex;gap:18px;align-items:flex-start;justify-content:space-between}.watch-detail-head h2{font-size:19px;line-height:1.5;max-width:680px;overflow-wrap:anywhere}.watch-detail-head .btn{flex:none}.watch-detail-head .caption{margin-top:6px}.watch-detail .watch-scope{margin-top:12px}.watch-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:20px;margin:24px 0}.watch-kpis strong{display:block;font-size:23px;line-height:1.3;font-weight:600;letter-spacing:-.4px;font-variant-numeric:tabular-nums}.watch-kpis span{display:block;font-size:12px;margin-top:6px;color:#687480}.watch-kpis .caption{margin:3px 0 0}.watch-tabs{display:flex;gap:4px;flex-wrap:wrap;border-bottom:1px solid var(--watch-line);margin-top:22px}.watch-tabs button{border:0;border-bottom:2px solid transparent;background:none;color:#78818a;padding:10px 12px;font:inherit;font-size:13px;cursor:pointer}.watch-tabs button.active{color:var(--watch-accent);border-color:var(--watch-accent)}.watch-tabs button:focus-visible,.watch-history-row:focus-visible,.watch-product-link:focus-visible{outline:2px solid var(--watch-accent);outline-offset:3px}.watch-range{display:flex;justify-content:flex-end;gap:6px;margin-top:14px}.watch-range button{padding:4px 10px}.watch-visual{margin:20px 0;break-inside:avoid}.watch-visual figcaption{font-size:13px;font-weight:600;margin-bottom:12px}.watch-visual svg{display:block!important;width:100%;height:auto;max-height:260px;stroke:none;overflow:visible}.watch-visual text{font:11px -apple-system,BlinkMacSystemFont,"PingFang SC",sans-serif;fill:#78818a;stroke:none}.watch-visual .watch-axis{fill:none;stroke:#dbe1e8;stroke-width:1}.watch-visual .watch-line{fill:none;stroke:var(--watch-accent);stroke-width:2}.watch-visual .watch-point,.watch-visual .watch-bar{fill:var(--watch-accent);stroke:none}.watch-visual .watch-point{opacity:.8}.watch-visual .watch-bar{opacity:.75}.watch-visual .watch-missing{fill:none;stroke:#aeb8c3;stroke-width:1.5}.watch-chart-empty{padding:28px 0;margin:12px 0;border-bottom:1px solid var(--watch-line);font-size:13px;color:#78818a}.watch-details{margin:18px 0 0;border-top:1px solid var(--watch-line);padding-top:15px}.watch-details summary{cursor:pointer;font-size:13px;color:#596572}.watch-details table{width:100%;font-size:12px;border-collapse:collapse;margin:14px 0}.watch-details td,.watch-details th{padding:9px 12px;border-bottom:1px solid var(--watch-line);text-align:left}.watch-details h3{font-size:14px;margin-top:22px}.watch-details .table-wrap{overflow:auto}.watch-history-day{display:flex;gap:16px;padding:16px 0;border-bottom:1px solid var(--watch-line);font-size:12px}.watch-history-day>h4{font-size:12px;font-weight:500;flex:0 0 90px;color:#78818a;padding-top:8px}.watch-history-day>div{flex:1;min-width:0}.watch-history-row{display:flex;align-items:center;justify-content:space-between;gap:16px;width:100%;text-align:left;border:0;border-radius:6px;background:none;padding:8px 10px;font:inherit;color:#596572;cursor:pointer}.watch-history-row.active{background:#f0f5fc;color:var(--watch-accent)}.watch-history-row small{color:#78818a}.watch-collection-head{display:flex;gap:14px;align-items:center;justify-content:space-between;margin:24px 0 12px}.watch-collection-head h2{font-size:16px}.watch-toolbar{flex-wrap:wrap}.watch-toolbar .search{flex:1;min-width:170px}.watch-list{border-top:1px solid var(--watch-line)}.watch-product-row{display:grid;grid-template-columns:minmax(220px,1fr) 110px 140px 120px;gap:20px;align-items:center;padding:16px 4px;border-bottom:1px solid var(--watch-line)}.watch-product-row.selected{background:#f7faff}.watch-product-link{display:flex;gap:12px;align-items:center;min-width:0;background:none;border:0;text-align:left;padding:0;font:inherit;color:inherit;cursor:pointer}.watch-product-link>span{min-width:0}.watch-product-link strong{font-size:13px;line-height:1.5;font-weight:550;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;overflow-wrap:anywhere}.watch-product-link .product-art{width:48px;height:52px}.watch-product-link small{display:block;font-size:11px;line-height:1.7;color:#78818a;margin-top:4px}.watch-row-value{font-size:14px;font-weight:600;font-variant-numeric:tabular-nums}.watch-row-value small{display:block;font-size:11px;font-weight:400;color:#78818a;margin-top:5px}.watch-spark .watch-visual{margin:0}.watch-spark .watch-visual svg{width:130px;max-width:100%;height:32px}.watch-spark .caption{font-size:10px;white-space:nowrap}.watch-row-actions{display:flex;flex-wrap:wrap;justify-content:flex-end;gap:7px}.watch-row-actions .btn{font-size:11px;padding:6px 9px}.watch-empty-hint{padding:22px 0;border-top:1px solid var(--watch-line);margin-bottom:24px}.watch-empty-hint h2{font-size:17px;margin-bottom:8px}.watch-export{font:14px/1.8 -apple-system,BlinkMacSystemFont,"PingFang SC",sans-serif}.watch-export>article{margin-top:32px}.watch-export .watch-details{break-inside:auto}.watch-export .watch-scope{margin:12px 0}.watch-export h3{line-height:1.6}
@media(max-width:1000px){.watch-product-row{grid-template-columns:minmax(180px,1fr) 95px 120px;gap:14px}.watch-spark{grid-column:2}.watch-row-actions{grid-column:3;grid-row:1 / span 2}.watch-product-link{grid-row:1 / span 2}}
@media(max-width:600px){.watch-detail-head{flex-direction:column;gap:12px}.watch-kpis{grid-template-columns:repeat(2,1fr);gap:20px 14px}.watch-kpis strong{font-size:21px}.watch-tabs button{padding:9px 8px;font-size:12px}.watch-product-row{grid-template-columns:minmax(0,1fr) 100px;gap:12px}.watch-product-link{grid-column:1 / -1;grid-row:auto}.watch-row-value{grid-column:1;grid-row:auto}.watch-spark{grid-column:2;grid-row:2}.watch-row-actions{grid-column:1 / -1;grid-row:auto;justify-content:flex-start}.watch-toolbar select{max-width:100%}.watch-toolbar .segmented{flex-wrap:wrap}.watch-history-day{display:block}.watch-history-day>h4{margin-bottom:8px}.watch-collection-head{align-items:flex-start}.watch-visual text{font-size:12px}.watch-page .watch-detail{scroll-margin-top:130px}}
@media print{.watch-export .watch-details>*{display:block!important}.watch-export .watch-details table{display:table!important}.watch-export .watch-details .table-wrap{overflow:visible}.watch-export summary{display:none!important}.watch-visual svg{max-height:none}.watch-export td,.watch-export th{white-space:normal}}
`;}
function syncWatchProject(){
 if(watch.projectId===state.project?.id)return;
 watch.projectId=state.project?.id||'';watch.productId='';watch.historyIndex=-1;watch.metric='price';watch.months='12';watch.filter=state.project?.products.some(x=>x.candidate)?'candidate':'all';
}
function watchPoints(points){
 const byTime=new Map();for(const x of points||[]){const time=Date.parse(x.at);if(Number.isFinite(time))byTime.set(time,{at:x.at,time,value:typeof x.value==='number'&&Number.isFinite(x.value)?x.value:null})}
 return [...byTime.values()].sort((a,b)=>a.time-b.time);
}
function watchLineChart(points,label,precision=2,{rank=false,mini=false}={}){
 const rows=watchPoints(points),valid=rows.filter(x=>x.value!==null);
 if(valid.length<2)return mini?'<span class="caption">价格记录不足</span>':`<p class="watch-chart-empty">${esc(label)}：有效时间点不足，暂不绘制趋势。缺失值不补零。</p>`;
 const width=mini?130:760,height=mini?32:225,left=mini?2:68,right=mini?128:735,top=mini?3:22,bottom=mini?29:178;
 const lo=Math.min(...valid.map(x=>x.value)),hi=Math.max(...valid.map(x=>x.value)),span=hi-lo||1,start=rows[0].time,end=rows.at(-1).time;
 const px=x=>left+(x.time-start)/(end-start||1)*(right-left),py=x=>hi===lo?(top+bottom)/2:rank?top+(x.value-lo)/span*(bottom-top):bottom-(x.value-lo)/span*(bottom-top);
 let drawing=false;const path=rows.map(x=>{if(x.value===null){drawing=false;return ''}const command=drawing?'L':'M';drawing=true;return command+px(x).toFixed(1)+','+py(x).toFixed(1)}).join(' ');
 const dots=valid.map(x=>`<circle class="watch-point" cx="${px(x).toFixed(1)}" cy="${py(x).toFixed(1)}" r="${mini?1:2.5}"><title>${esc(x.at.slice(0,10))}：${watchValue(x.value,precision)}</title></circle>`).join('');
 return `<figure class="watch-visual">${mini?'':`<figcaption>${esc(label)}</figcaption>`}<svg viewBox="0 0 ${width} ${height}" role="img" aria-label="${esc(label)}，按实际日期绘制">${mini?'':`<path class="watch-axis" d="M${left} ${top}V${bottom}H${right}"/><text x="${left-9}" y="${top+4}" text-anchor="end">${watchValue(rank?lo:hi,precision)}</text><text x="${left-9}" y="${bottom+4}" text-anchor="end">${watchValue(rank?hi:lo,precision)}</text><text x="${left}" y="207">${esc(rows[0].at.slice(0,10))}</text><text x="${right}" y="207" text-anchor="end">${esc(rows.at(-1).at.slice(0,10))}</text>`}<path class="watch-line" data-series="line" d="${path}"/>${dots}</svg>${mini?'':`<p class="caption">按实际日期和返回时间点绘制；缺失值处断开，不补零。${rank?'BSR 数字越小，名次越前；仅查看此商品，不跨类目比较。':''}</p>`}</figure>`;
}
function priceChart(points,label='历史价格 · USD'){return watchLineChart(points,label)}
function historyPriceChart(points,months){
 if(watchPoints(points).filter(x=>x.value!==null).length>=2)return priceChart(points);
 const average=(months||[]).map(x=>({at:x.month+'-01T00:00:00Z',value:x.average_price}));
 return watchPoints(average).filter(x=>x.value!==null).length>=2?'<p class="caption">近 90 天价格记录不足，以下展示供应商返回的月均价格；两者统计口径不同。</p>'+priceChart(average,'月均价格 · USD'):priceChart(points);
}
function watchSalesChart(months,metric,range='12'){
 const all=(months||[]).filter(x=>/^20\d{2}-(0[1-9]|1[0-2])$/.test(x.month)).slice().sort((a,b)=>a.month.localeCompare(b.month));
 const ordinal=month=>Number(month.slice(0,4))*12+Number(month.slice(5))-1,last=all.length?ordinal(all.at(-1).month):0;
 const rows=range==='all'?all:all.filter(x=>ordinal(x.month)>last-12),label=WATCH_METRICS[metric][1],values=rows.filter(x=>typeof x[metric]==='number'&&Number.isFinite(x[metric]));
 if(!values.length)return `<p class="watch-chart-empty">${esc(label)}：当前范围未提供有效数据。缺失月份不补零。</p>`;
 const start=ordinal(rows[0].month),count=last-start+1,max=Math.max(...values.map(x=>x[metric])),span=max||1,step=667/count,bar=Math.max(1,Math.min(38,step*.65));
 const marks=rows.map(x=>{const cx=68+(ordinal(x.month)-start+.5)*step,v=x[metric],ok=typeof v==='number'&&Number.isFinite(v),y=ok?178-v/span*150:178;return ok?`<rect class="watch-bar" data-month="${x.month}" data-value="${v}" x="${(cx-bar/2).toFixed(1)}" y="${y.toFixed(1)}" width="${bar.toFixed(1)}" height="${(178-y).toFixed(1)}"><title>${x.month}：${watchValue(v)} 件（估算）</title></rect>${v===0?`<circle class="watch-point" cx="${cx.toFixed(1)}" cy="178" r="2"><title>${x.month}：0 件（估算）</title></circle>`:''}`:`<path class="watch-missing" d="M${(cx-3).toFixed(1)} 175l6 6m-6 0l6-6"><title>${x.month}：未提供</title></path>`}).join('');
 const ticks=Array.from({length:count},(_,i)=>i).filter(i=>i===0||i===count-1||(count<=12?i%2===0:i%Math.ceil(count/6)===0)).map(i=>{const n=start+i,month=Math.floor(n/12)+'-'+String(n%12+1).padStart(2,'0');return `<text x="${(68+(i+.5)*step).toFixed(1)}" y="207" text-anchor="middle">${month}</text>`}).join('');
 return `<figure class="watch-visual"><figcaption>${label}</figcaption><svg viewBox="0 0 760 225" role="img" aria-label="${label}，月份缺失不补零"><path class="watch-axis" d="M68 22V178H735"/><text x="59" y="30" text-anchor="end">${watchValue(max)}</text><text x="59" y="182" text-anchor="end">0</text>${marks}${ticks}</svg><p class="caption">${range==='all'?'全部可用月份':'最近 12 个月（截至本次返回的最新月份）'}；空档表示未提供，× 表示该月值缺失。父体包含多个变体，父子体不相加；估算量不等于真实订单。最新月份可能尚未结束。</p></figure>`;
}
function watchAsinNote(result,sales=false){
 if(result?.status!=='success')return '';
 const data=result.data,linked=[...new Set([data.returned_asin,...(sales?[]:[data.data_asin])].filter(x=>x&&x!==data.requested_asin))];
 if(!linked.length)return '';
 return sales?`<p class="source-note">销量返回商品 ${linked.map(esc).join('、')} 与查询商品不同，请先核对父子体关联。图表标注为供应商返回商品，不能当作所选商品的走势。</p>`:`<p class="source-note">供应商返回关联商品 ${linked.map(esc).join('、')}，与所选 ${esc(data.requested_asin)} 不同，不能直接作为所选商品的走势。</p>`;
}
function watchChartFor(history,metric,range='12'){
 const keepa=historyResult(history,'keepa_info'),sales=historyResult(history,'asin_sales_trend'),monthly=metric.endsWith('_units'),result=monthly?sales:keepa;
 if(metric==='price'&&keepa?.status!=='success'&&sales?.status==='success'&&sales.data.returned_asin===sales.data.requested_asin)return `<p class="caption">图表数据 ASIN：${esc(sales.data.returned_asin)}</p>`+historyPriceChart([],sales.data.months);
 if(result?.status!=='success')return `<p class="watch-chart-empty">${WATCH_METRICS[metric][0]}尚无成功查询结果，请查看本次查询状态。</p>`;
 const data=result.data,asin=data.data_asin||data.returned_asin||data.requested_asin,context=`<p class="caption">图表数据 ASIN：${esc(asin)}${data.parent_asin?' · 父体 '+esc(data.parent_asin):''}</p>`;
 if(monthly)return context+watchSalesChart(data.months,metric,range);
 const months=sales?.status==='success'&&sales.data.returned_asin===data.requested_asin&&!watchAsinNote(keepa)?sales.data.months:[];
 return context+(metric==='price'?historyPriceChart(data.series?.price,months):watchLineChart(data.series?.[metric],WATCH_METRICS[metric][1],WATCH_METRICS[metric][2],{rank:metric==='bsr'}));
}
function watchRawDetails(keepa,sales){
 const series=keepa?.status==='success'?keepa.data.series||{}:{},months=sales?.status==='success'?sales.data.months||[]:[],missingPoints=Object.values(series).reduce((sum,points)=>sum+points.filter(x=>x.value===null).length,0),missingSales=months.reduce((sum,x)=>sum+['parent_units','child_units','average_price','price','parent_revenue','child_revenue'].filter(k=>x[k]==null).length,0);
 return `${keepa?.status==='success'?`<details class="watch-details"><summary>查看价格、排名与评分时间点</summary><p class="caption">跳过异常、重复或超出范围时间点 ${watchValue(keepa.data.skipped_points)} 个；保留缺失值时间点 ${watchValue(missingPoints)} 个，显示「未提供」。</p>${Object.entries(series).map(([name,points])=>`<h3>${esc(WATCH_METRICS[name]?.[1]||name)}</h3><div class="table-wrap"><table><tr><th>日期（UTC）</th><th>记录值</th></tr>${points.map(x=>`<tr><td>${esc(x.at)}</td><td>${watchValue(x.value,WATCH_METRICS[name]?.[2]||0)}</td></tr>`).join('')}</table></div>`).join('')}</details>`:''}${sales?.status==='success'?`<details class="watch-details"><summary>查看全部 ${months.length} 个月的销量与价格明细</summary><p class="caption">供应商全部可用月份，不限于近 90 天。父体包含多个变体，子体对应具体商品；销量与销售额均为第三方估算，不等于真实订单，价格与金额为美元。「供应商价格」保留原 price 字段，不推定为月末价格。</p><p class="caption">跳过异常或重复月份 ${watchValue(sales.data.skipped_points)} 个；当前表格缺失值 ${watchValue(missingSales)} 项，显示「未提供」，不补零。</p><div class="table-wrap"><table><tr><th>月份</th><th>父体月销量</th><th>子体月销量</th><th>月均价</th><th>供应商价格</th><th>父体月销售额估算</th><th>子体月销售额估算</th></tr>${months.map(x=>`<tr><td>${esc(x.month)}</td><td>${watchValue(x.parent_units)}</td><td>${watchValue(x.child_units)}</td><td>${watchValue(x.average_price,2)}</td><td>${watchValue(x.price,2)}</td><td>${watchValue(x.parent_revenue,2)}</td><td>${watchValue(x.child_revenue,2)}</td></tr>`).join('')}</table></div></details>`:''}`;
}
function historyBody(history,{exported=false,metric=watch.metric,months=watch.months,modalView=false}={}){
 if(!history)return '';
 const keepa=historyResult(history,'keepa_info'),sales=historyResult(history,'asin_sales_trend'),series=keepa?.status==='success'?keepa.data.series||{}:{},statuses={running:'查询中',success:'已保存',partial:'部分结果已保存',failed:'查询失败',interrupted:'查询中断'};
 const kpis=keepa?.status==='success'?`<div class="watch-kpis">${[['最新记录价格 · USD',series.price,2],['BSR 排名',series.bsr,0],['评分',series.rating,1],['评分数',series.ratings_count,0]].map(([label,points,precision])=>{const x=lastPoint(points);return `<div><strong>${watchValue(x?.value,precision)}</strong><span>${label}</span><p class="caption">${x?esc(x.at.slice(0,10)):'未提供'}</p></div>`}).join('')}</div>`:'';
 const controls=exported?'':`<nav class="watch-tabs" aria-label="竞品趋势指标">${Object.entries(WATCH_METRICS).map(([key,[label]])=>`<button class="${metric===key?'active':''}" aria-pressed="${metric===key}" data-action="${modalView?'watch-modal-metric':'watch-metric'}" data-value="${key}">${label}</button>`).join('')}</nav>${metric.endsWith('_units')?`<div class="watch-range segmented" aria-label="月销量时间范围">${[['12','近 12 个月'],['all','全部月份']].map(([value,label])=>`<button class="${months===value?'active':''}" data-action="${modalView?'watch-modal-months':'watch-months'}" data-value="${value}">${label}</button>`).join('')}</div>`:''}`;
 const charts=exported?Object.keys(WATCH_METRICS).map(metric=>watchChartFor(history,metric,'all')).join(''):watchChartFor(history,metric,months);
 return `<p class="report-meta">${esc(statuses[history.status]||history.status)} · ${date(history.finished_at||history.created_at)} · 手动查询 ${watchValue(history.requests)} / ${watchValue(history.request_limit)} 次 · ${esc(history.source)}</p>${(history.results||[]).filter(x=>x.status==='failed').map(x=>`<p class="error-inline">${x.tool==='keepa_info'?'价格与排名':'月度销量'}：${esc(x.error)}${x.http?'（HTTP '+Number(x.http)+'）':''}</p>`).join('')}${watchAsinNote(keepa)}${watchAsinNote(sales,true)}${kpis}${controls}${charts}<p class="caption">评分数是供应商的评分数量，不等于本项目已采集的评论条数。所有曲线均为已保存记录，未开启定时监控。</p>${watchRawDetails(keepa,sales)}`;
}
function watchSelectedHistory(id){const selected=state.project.watch_history?.[watch.historyIndex];return selected?.product_id===id?selected:latestHistory(id)}
function watchHistoryTimeline(id){
 const rows=(state.project.watch_history||[]).map((record,index)=>({record,index})).filter(x=>x.record.product_id===id).sort((a,b)=>(b.record.created_at||'').localeCompare(a.record.created_at||'')||b.index-a.index);
 if(!rows.length)return '';
 const groups=new Map();for(const row of rows){const day=(row.record.created_at||'日期未提供').slice(0,10);if(!groups.has(day))groups.set(day,[]);groups.get(day).push(row)}
 const selected=watchSelectedHistory(id),statuses={success:'已保存',partial:'部分结果',failed:'失败',running:'查询中',interrupted:'中断'};
 return `<details class="watch-details"><summary>查询记录 · ${rows.length} 次</summary><p class="caption">按查询日期分组（UTC），点击回看保存的快照，不会再次查询。</p>${[...groups.entries()].map(([day,items])=>`<section class="watch-history-day"><h4>${esc(day)}</h4><div>${items.map(({record,index})=>`<button class="watch-history-row ${record===selected?'active':''}" data-action="watch-snapshot" data-index="${index}" aria-pressed="${record===selected}"><span>${esc((record.created_at||'').slice(11,16)||'时间未提供')} · ${esc(statuses[record.status]||record.status)}</span><small>${watchValue(record.requests)} 次查询${record===selected?' · 正在查看':''}</small></button>`).join('')}</div></section>`).join('')}</details>`;
}
function watchHistorySection(){
 const p=state.project,id=p.products.some(x=>x.id===watch.productId)?watch.productId:p.watch_history?.at(-1)?.product_id,product=p.products.find(x=>x.id===id);
 if(!product)return `<section class="watch-empty-hint"><h2>查看竞品变化</h2><p class="caption">在下方选择一件商品，集中查看价格、BSR、评分与月度销量估算。当前按需查询，未开启定时监控。</p></section>`;
 const history=watchSelectedHistory(id);
 return `<section class="watch-detail" id="watch-history"><div class="watch-detail-head"><div><h2>${esc(product.title)}</h2><p class="caption">${esc(product.asin||product.id)} · 美国站 · ${external(product.source_url,'商品页面')}</p></div>${button(watch.busy?'正在查询…':latestHistory(id)?'更新趋势 · 2次查询':'读取历史 · 2次查询','refresh-product-history','refresh','primary',`data-id="${esc(id)}" ${watch.busy?'disabled':''}`)}</div><p class="caption watch-scope">读取近 90 天的价格、BSR 与评分记录，以及供应商全部可用月份的销量估算；每次保存到当前项目。未开启定时监控。</p>${history?historyBody(history):'<p class="watch-chart-empty">还没有查询这件竞品的历史。点击「读取历史」开始，只查询本次所选商品。</p>'}${watchHistoryTimeline(id)}</section>`;
}
function watchSpark(product){
 const h=latestHistory(product.id),keepa=historyResult(h,'keepa_info'),sales=historyResult(h,'asin_sales_trend');let points=[],label='历史价格';
 if(keepa?.status==='success'&&!watchAsinNote(keepa))points=keepa.data.series?.price||[];
 if(watchPoints(points).filter(x=>x.value!==null).length<2&&sales?.status==='success'&&!watchAsinNote(sales,true)){points=(sales.data.months||[]).map(x=>({at:x.month+'-01T00:00:00Z',value:x.average_price}));label='月均价格'}
 if(watchPoints(points).filter(x=>x.value!==null).length<2)return '<span class="caption">尚无价格曲线</span>';
 const last=lastPoint(watchPoints(points));return watchLineChart(points,label+' · USD',2,{mini:true})+`<p class="caption">${label} · ${esc(last.at.slice(0,10))}</p>`;
}
function watchProductList(){
 const p=state.project,candidates=p.products.filter(x=>x.candidate),query=(state.query||'').toLowerCase();let rows=p.products.filter(x=>(x.title+' '+(x.features||[]).join(' ')+' '+(x.brand||'')).toLowerCase().includes(query));
 if(watch.filter==='candidate')rows=rows.filter(x=>x.candidate);
 if(state.sort==='price')rows.sort((a,b)=>(a.currency||'未知').localeCompare(b.currency||'未知')||(a.price??Infinity)-(b.price??Infinity));
 if(state.sort==='rating')rows.sort((a,b)=>(b.rating??-1)-(a.rating??-1));
 const selected=watch.productId||p.watch_history?.at(-1)?.product_id;
 return `<div class="watch-collection-head"><h2>竞品清单</h2>${button('并排比较','compare','compare','small',candidates.length?'':'disabled')}</div><div class="toolbar watch-toolbar"><div class="segmented">${[['candidate','关注竞品',candidates.length],['all','全部商品',p.products.length]].map(([value,label,count])=>`<button class="${watch.filter===value?'active':''}" data-action="watch-filter" data-value="${value}">${label} ${count}</button>`).join('')}</div><div class="search">${icon('search')}<input id="search-input" placeholder="筛选名称、品牌或特点" aria-label="筛选商品" value="${esc(state.query)}"></div><select id="sort-select" aria-label="商品排序"><option value="default">默认顺序</option><option value="price" ${state.sort==='price'?'selected':''}>同币种价格从低到高</option><option value="rating" ${state.sort==='rating'?'selected':''}>评分从高到低</option></select>${button('导出商品','export-products','download','small')}</div>${rows.length?`<div class="watch-list">${rows.map(x=>{const supported=x.platform==='amazon'||x.asin;return `<article class="watch-product-row ${x.id===selected?'selected':''}" data-product="${esc(x.id)}"><button class="watch-product-link" data-action="${supported?'product-history':'product-detail'}" data-id="${esc(x.id)}" aria-label="${supported?'查看趋势：':'查看商品：'}${esc(x.title)}">${art(x)}<span><strong>${esc(x.title)}</strong><small>${esc(x.brand||x.query||x.original_query||'')}${x.brand||x.query||x.original_query?' · ':''}${esc(x.source||'来源未注明')}</small></span></button><div class="watch-row-value">${money(x)}<small>评分 ${watchValue(x.rating,1)} · ${watchValue(x.review_count)} 评</small></div><div class="watch-spark">${watchSpark(x)}</div><div class="watch-row-actions">${button(x.candidate?'✓ 已关注':'＋ 关注','candidate','','small',`data-id="${esc(x.id)}"`)}${button('商品资料','product-detail','','small',`data-id="${esc(x.id)}"`)}</div></article>`}).join('')}</div><div class="table-footer"><span>显示 ${rows.length} / ${p.products.length} 件商品</span><span>点击商品查看趋势 · 缺失数据不补零</span></div>`:empty('暂无符合条件的商品',watch.filter==='candidate'?'在全部商品中关注竞品，之后会优先展示在这里。':query?'当前仅筛选已保存商品。':'先按关键词查找竞品，或导入商品资料。',watch.filter==='candidate'?button('查看全部商品','watch-filter','','small','data-value="all"'):button('按关键词找竞品','query-products','search','primary'),'box')}`;
}
function marketWatch(){syncWatchProject();return `<style>${watchReportStyles()}</style><div class="watch-page">${head('电商盯盘','关注值得验证的竞品，查看价格、排名与销量估算的变化。',button('导入商品','import','upload','','data-kind="products"')+button('按关键词找竞品','query-products','search','primary'))}${demoNote()}${jobView()}${watchHistorySection()}${watchProductList()}</div>`}
function watchReportHtml(){const p=state.project,ids=[...new Set((p.watch_history||[]).map(x=>x.product_id))];return ids.length?`<style>${watchReportStyles()}</style><section class="watch-export"><h2>竞品历史观察</h2><p>每件商品展示最近一次手动查询。未开启定时监控；父子体销量均为供应商估算。</p>${ids.map(id=>{const product=p.products.find(x=>x.id===id);return `<article><h3>${esc(product?.title||id)}</h3>${historyBody(latestHistory(id),{exported:true})}</article>`}).join('')}</section>`:''}
function openWatchModal(product){watch.modalView={projectId:state.project.id,product,metric:'price',months:'12',token:String(++watch.modalSequence)};renderWatchModal()}
function watchModalCurrent(view){return view&&state.project?.id===view.projectId&&watch.modalView?.token===view.token&&state.modal&&document.querySelector('[data-watch-modal="'+view.token+'"]')}
function renderWatchModal(){
 const view=watch.modalView;if(!view||state.project?.id!==view.projectId)return;
 const product=view.product,history=latestHistory(product.id),saved=state.project.products.some(x=>x.id===product.id);
 modal('竞品趋势 · '+product.title,`<style>${watchReportStyles()}</style><section class="watch-export" data-watch-modal="${view.token}"><p class="caption">${esc(product.id)} · ${external(product.source_url,'商品来源')}</p>${history?historyBody(history,{metric:view.metric,months:view.months,modalView:true}):'<p class="watch-chart-empty">尚未保存这件商品的历史曲线。商品推荐已经完成，历史趋势可按需补查。</p>'}<p class="caption">手动更新近 90 天价格、BSR 与评分记录，以及月度销量估算；每次 2 次 MCP 查询。</p>${!saved?'<p class="caption">当前项目没有这件商品资料，暂不能更新历史。</p>':''}</section>`,button(watch.busy?'正在查询…':history?'更新趋势 · 2次查询':'读取历史 · 2次查询','watch-modal-refresh','refresh','',watch.busy||!saved?'disabled':'')+button('关闭','close','','primary'),true);
}
async function watchAction(action,el){switch(action){
 case'watch-modal-metric':if(watchModalCurrent(watch.modalView)&&Object.hasOwn(WATCH_METRICS,el.dataset.value)){watch.modalView.metric=el.dataset.value;renderWatchModal()}return true;
 case'watch-modal-months':if(watchModalCurrent(watch.modalView)&&['12','all'].includes(el.dataset.value)){watch.modalView.months=el.dataset.value;renderWatchModal()}return true;
 case'watch-modal-refresh':{const view=watch.modalView;if(watch.busy||!watchModalCurrent(view))return true;watch.busy=true;renderWatchModal();try{const project=await api('/api/projects/'+view.projectId+'/product-history',{revision:state.project.revision,product_id:view.product.id});if(state.project?.id===view.projectId&&project.revision>=state.project.revision)state.project=project}finally{watch.busy=false;if(watchModalCurrent(view))renderWatchModal();if(state.project?.id===view.projectId)render()}return true}
 case'watch-product-direction':case'query-products':{const query=String(action==='watch-product-direction'?el.dataset.query||'':state.project?.keyword||'').trim();if(action==='watch-product-direction'&&!query)throw Error('缺少商品方向查询词');syncWatchProject();watch.filter='all';goto('products');collectModal('posts',query,['amazon']);return true}
 case'product-history':syncWatchProject();watch.productId=el.dataset.id;watch.historyIndex=-1;watch.metric='price';if(state.page==='products')render();else goto('products');document.querySelector('#watch-history')?.scrollIntoView({behavior:'auto',block:'start'});return true;
 case'watch-filter':if(['all','candidate'].includes(el.dataset.value)){watch.filter=el.dataset.value;render()}return true;
 case'watch-metric':if(Object.hasOwn(WATCH_METRICS,el.dataset.value)){watch.metric=el.dataset.value;render()}return true;
 case'watch-months':if(['12','all'].includes(el.dataset.value)){watch.months=el.dataset.value;render()}return true;
 case'watch-snapshot':{const index=Number(el.dataset.index),record=Number.isInteger(index)&&state.project.watch_history?.[index],id=watch.productId||state.project.watch_history?.at(-1)?.product_id;if(record&&record.product_id===id){watch.historyIndex=index;render();document.querySelector('#watch-history')?.scrollIntoView({behavior:'auto',block:'start'})}return true}
 case'refresh-product-history':{if(watch.busy)return true;const id=state.project.id,productId=el.dataset.id;watch.busy=true;render();try{const p=await api('/api/projects/'+id+'/product-history',{revision:state.project.revision,product_id:productId});if(state.project?.id===id){state.project=p;if(watch.productId===productId||!watch.productId)watch.historyIndex=-1}const r=p.watch_history?.at(-1);toast(r?.status==='success'?'竞品历史已保存':r?.status==='partial'?'部分历史已保存，请查看失败说明':'查询未完成，请查看原因')}finally{watch.busy=false;render();if(state.project?.id===id){document.querySelector('#watch-history')?.scrollIntoView({behavior:'auto',block:'start'})}}return true}
 default:return false;
}}
