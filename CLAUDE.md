# 远见工作台开发说明

项目通用规范见 `AGENTS.md`，两者共同约束开发；用户当前明确授权优先。

- Python 3.10+ 标准库与原生 JavaScript，不增加运行依赖。
- 使用 `team_start.py` 启动；保留远端已有的 Windows 支持和 Codex 服务提供方、认证与工具隔离设置。
- 三个主入口为「关键词洞察」「电商盯盘」「报告与选品」，采集、分析、翻译在页面内继续。
- 采词模式为快速、A～Z、意图深挖。快速一轮，高级模式可选 1～6 轮；后续轮次只查询真实返回的新词，规则构造的查询词不能冒充采集结果。
- 显示请求上限、实际调用数、每平台和每轮结果；达到预算、取消或接口失败时如实保存，不无限重试。
- 洞察可选择关键词库、人群画像、搜索意图、评论需求、内容规律、营销选题、选品建议，逐块保存并允许单块重跑；`summary` 仍是选品建议的存储键，不迁移旧报告。
- 报告先给结论，再用图表展示数量和分布；完整画像按人群折叠，原始证据与方法说明按需展开。逐词分类和逐词内容建议不展示，分类仅用于后台汇总；不得因此删除原始资料。
- 选品建议最多 3 个 `product_directions`，证据不足可为空。每项须有目标人群、问题依据、待验证差异点、检索词和核查事项；不得把假设写成已验证性能或利润。检索词标明 AI 构造，不混入采集词统计。
- 按用户最新要求，洞察成功后自动接续卖家精灵商品查询与本机 Codex 推荐，旧报告首次进入阅读页时补齐；无需额外查询点击或跳转电商盯盘。每份报告最多查询 2 个 Amazon 美国站关键词，每词最多 10 件商品，从真实结果中给出 1 个优先参考商品与至多 2 个备选；没有匹配时如实说明。
- 自动推荐按报告内容持久去重，已保存结果直接复用；失败或中断不自动重复消费。纯渲染、导出、历史回看与初始化检查不得触发查询。报告页原地阅读推荐、商品对比、依据及完整分析；更新商品趋势仍由用户在原地明确发起。
- 每块报告保留样本范围、证据快照、提示词版本与状态。人群画像区分有据结论、推断和未知，不编造年龄、收入、购买行为或用户原话。
- 界面、HTML 和 Markdown 使用同一报告内容结构。HTML 内置样式与图表，保留可展开的画像、原始依据、推荐商品快照和来源；兼容旧项目和旧报告。旧报告缺少 `product_directions` 时用其真实需求和原文补齐推荐，不能虚构商品或把假设写成已验证结论。
- 图表必须区分零与缺失，按真实日期绘制历史，缺失点不补零；关键词数量不能转成消费者比例。父子体销量和销售额分别展示，不相加。
- 中文输入法组词期间不得替换输入框；跨项目异步结果不能写入当前其他项目。
- 数据版本遵循 SHA-256 和 revision 校验，不手工覆盖历史。测试写入仅用临时目录。
- Git 只同步代码。连接、登录、本机配置、业务数据和导出报告必须留在忽略范围内。
- 团队在本地 `main` 开发；完成验证后提交，通过独立远端分支向上游 `main` 提 PR，不能因无上游推送权限而直接覆盖历史。
- 完成后同步 `ROADMAP.md`，只记录已验证的能力，不将模拟测试写成真实外部调用成功。
